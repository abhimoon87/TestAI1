# Core dependencies
customtkinter>=5.2.0
Pillow>=9.0.0  # required by the GUI (CTkImage / PIL.Image)
pandas>=2.0.0
numpy>=1.24.0
yfinance>=0.2.30

# Data providers (free, no auth needed)
jugaad-data>=0.35.0
nselib>=2.5.0

# HTTP requests (for API calls and news fetching)
requests>=2.28.0
