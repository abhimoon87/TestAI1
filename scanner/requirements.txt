# Core dependencies
# NOTE: flet is pinned to the latest stable (1.0.0, 2026-09-14) — the GUI is
# built and tested against this version's control API (page.open/close and
# page.snack_bar were removed in 1.0 in favor of show_dialog/pop_dialog).
flet==1.0.0
Pillow>=9.0.0
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0
yfinance>=0.2.30

# Data providers (free, no auth needed)
jugaad-data>=0.35.0
nselib>=2.5.0

# HTTP requests (for API calls and news fetching)
requests>=2.28.0

# Animated loading spinners
flet-spinkit>=1.0.0
